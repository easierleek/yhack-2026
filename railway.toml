[build]
builder = "nixpacks"
buildCommand = "pip3 install -r requirements.txt && cd frontend/web && npm ci && npm run build"

[deploy]
startCommand = "python3 server.py"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
