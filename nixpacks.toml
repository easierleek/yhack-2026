[phases.setup]
nixPkgs = ["python312", "nodejs_22"]

[phases.install]
cmds = [
  "python3 -m venv /opt/venv",
  "/opt/venv/bin/pip install -r requirements.txt",
  "cd frontend/web && npm ci"
]

[phases.build]
cmds = ["cd frontend/web && npm run build"]

[start]
cmd = "/opt/venv/bin/python3 server.py"
